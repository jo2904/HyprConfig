# Secrets & credentials
*.pem
*.key
*.env
.env*
credentials.json
**/secrets/
**/.ssh/

# Waybar generated files
config/waybar/temp.json
config/waybar/*.backup

# Rofi : réécrit à chaque bascule de thème par toggle-theme.sh (le contenu
# réel vient de -theme-str dans launch.sh, ce fichier n'a pas d'effet propre)
config/rofi/colors.rasi

# Hypridle : réécrit à chaque lancement par start-hypridle.sh selon la
# présence d'une batterie (verrouillage 30min PC fixe / 3min PC portable)
config/hypr/hypridle-lock-timeout.conf

# OS & editor
.DS_Store
*.swp
*.swo
*~
.vscode/
